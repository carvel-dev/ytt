#@ load("@ytt:overlay", "overlay")

#! From all documents, select those with id: 1
#@overlay/match by=overlay.subset({"id": 1})
---
awards:
  #@overlay/match by=overlay.index(0)
  #@overlay/insert before=True
  - Ovid Prize
  #@overlay/match by=overlay.index(0)
  #@overlay/insert before=True
  - Grand Prix of Literary Associations

#! each operation on an array requires two parts:
#! 1. a matcher that selects which item(s) to edit
#! 2. an action that describes the edit
#!
#! #@overlay/append is short-hand for:
#!   #@overlay/match by=overlay.index(len(left)-1)
#!   #@overlay/insert after=True