#@ load("@ytt:overlay", "overlay")

#! this matcher select which *documents* to overlay
#@overlay/match by=overlay.all, expects="1+"
---
#! inside an overlay, the default action is @overlay/merge
#! so, matching documents *must* have a `items` key
items:
#@overlay/match by="id"
- id: 3
  modified: true
