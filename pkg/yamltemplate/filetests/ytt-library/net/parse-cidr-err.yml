#@ load("@ytt:net", "net")

ipv4:
  cidr: #@ net.parse_cidr("not-a-valid-cidr")

+++

ERR: 
- net.parse_cidr: invalid CIDR address: not-a-valid-cidr
    in <toplevel>
      stdin:4 |   cidr: #@ net.parse_cidr("not-a-valid-cidr")