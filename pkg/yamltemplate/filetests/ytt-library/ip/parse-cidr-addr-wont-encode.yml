#@ load("@ytt:ip", "ip")

#@ addr, net = ip.parse_cidr("192.168.0.1/24")
net: #@ net

+++

ERR: 
- __ytt_tplXXX_set_node: Unable to convert value: IPNetValue does not automatically encode (hint: use .string())
    in <toplevel>
      stdin:4 | net: #@ net