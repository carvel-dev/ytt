#@ load("@ytt:url", "url")

url_value: #@ {"key": url.parse("http://example.com")}

+++

ERR: 
- __ytt_tplXXX_set_node: URLValue: does not automatically encode; use .string()
    in <toplevel>
      stdin:3 | url_value: #@ {"key": url.parse("http://example.com")}