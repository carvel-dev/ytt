#@ load("@ytt:assert", "assert")

string_not_null: #@ assert.not_null().check()

+++

ERR:
- function lambda missing 1 argument (value)
    in lambda
      assert.not_null:? | __ytt_tplXXX_set_ctx_type("yaml") (generated)
      stdin:1 | #@ load("@ytt:assert", "assert")
    in <toplevel>
      stdin:3 | string_not_null: #@ assert.not_null().check()