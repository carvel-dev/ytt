#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

test_equals: #@ assert.equals({"foo": "bar"}, {"foo": "baz"})

+++

ERR: 
- assert.equals: {"foo": "baz"} is not equal to the expected value {"foo": "bar"}
    in <toplevel>
      stdin:4 | test_equals: #@ assert.equals({"foo": "bar"}, {"foo": "baz"})