#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

test_equals: #@ assert.equals(1, 2)

+++

ERR: 
- assert.equals: 2 is not equal to the expected value 1
    in <toplevel>
      stdin:4 | test_equals: #@ assert.equals(1, 2)