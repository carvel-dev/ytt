#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

test_equals: #@ assert.equals(True, False)

+++

ERR: 
- assert.equals: False is not equal to the expected value True
    in <toplevel>
      stdin:4 | test_equals: #@ assert.equals(True, False)