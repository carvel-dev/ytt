#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

test_equals: #@ assert.equals("foo", "bar")

+++

ERR: 
- assert.equals: "bar" is not equal to the expected value "foo"
    in <toplevel>
      stdin:4 | test_equals: #@ assert.equals("foo", "bar")