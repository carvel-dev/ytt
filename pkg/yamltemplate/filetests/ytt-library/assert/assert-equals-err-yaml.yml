#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

#@ def yamlFunc():
foo: bar
#@ end

#@ def moreYamlFunc():
bar: foo
#@ end

test_equals: #@ assert.equals(yamlFunc(), moreYamlFunc())

+++

ERR: 
- assert.equals: yamlfragments are not equal:
    in <toplevel>
      stdin:12 | test_equals: #@ assert.equals(yamlFunc(), moreYamlFunc())

    reason:
     Expected:
     foo: bar
     -----
     Actual:
     bar: foo
