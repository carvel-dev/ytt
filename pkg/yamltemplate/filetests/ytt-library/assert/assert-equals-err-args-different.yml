#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

test_equals: #@ assert.equals("one", 1)

+++

ERR: 
- assert.equals: arguments are of different types. expected string, but got int
    in <toplevel>
      stdin:4 | test_equals: #@ assert.equals("one", 1)