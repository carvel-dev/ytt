#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

test_equals: #@ assert.equals("foo")

+++

ERR: 
- assert.equals: expected two arguments
    in <toplevel>
      stdin:4 | test_equals: #@ assert.equals("foo")