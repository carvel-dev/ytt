#@ load("@ytt:assert", "assert")
#@ load("@ytt:json", "json")

test_equals: #@ assert.equals([1, 2], [2, 3])

+++

ERR: 
- assert.equals: [2, 3] is not equal to the expected value [1, 2]
    in <toplevel>
      stdin:4 | test_equals: #@ assert.equals([1, 2], [2, 3])